# TTS Director Skill 路线图（更新版）

## 项目背景

### 发现的产品缝隙

Fish Audio 的网页端（Drama 3 模型）提供了"自动加标签"功能，可以自动为文本插入情感和韵律标签。但 API 端（S2.1 Pro 模型）不提供这个功能——开发者需要自己在文本中手动插入标签。

这个 skill 的目标是填补这个缝隙：让 Claude 自动分析中文文本的情感结构，在合适的位置插入 Fish Audio S2 Pro 支持的方括号标签，输出可以直接用于 API 调用的标注文本。

### 核心设计原则

在实验过程中形成的三个核心洞察：

1. **克制优先**：对于中文日常对话，过度标注会让语音像在演戏。默认策略是不标注，只在必要时插入标签。
2. **表里一致性判断**：标注密度不取决于情绪强度，而取决于文本表面语义和说话者真实意图之间的距离。表里一致时克制，表里不一致时密集。
3. **文本改写与标签同等重要**：断长句、加语气词、调语序、允许半句重来——这些口语化改写往往比加标签更能改善自然度。

### 音色与标签的交互效应

实验发现：在加了标签的情况下，不同音色并不容易导致角色脱离（OOC），标签效果相对稳定。关键因素是克隆声音源本身的基调，而非标签与音色的交互。因此 skill 没有采用最初设计的三档音色系统，而是统一处理。

---

## 进度追踪

### ✅ 第一步：Fish Audio 标签实验（已完成）

完成内容：
- 标签分类：情绪类、副语言类、语音语调类、动词短语型、复合描述性标签
- 发现稳定标签子集 vs 不稳定标签
- 密度规则：取决于表里一致性，而非情绪强度
- 音色交互：标签效果跨音色相对稳定
- 中文特殊发现：标点符号可替代标签、口腔微动作词直接触发韵律、"口非心是"软化策略
- S2 Pro 不支持环境音标签
- 大量连续破折号拖长音效果不佳
- 动词标签通常比情绪标签更有效
- 标签堆叠上限为 2 个
- `[thoughtful]` 需搭配语气词才稳定
- 强度调节修饰语（slightly / very / extremely）有效

产出：测试总结文档 + ElevenLabs 技巧参考截图

### ✅ 第二步：设计 Skill 规则（已完成）

核心规则框架：
- 四级工具优先级：标点符号 → 口腔微动作词 → 结构性标签 → 情绪/语调标签
- 五种文本类型：平叙型、知识解释型、情感错位型、情绪急转弯型、高表现力型
- 四步标注流程：通读 → 口语化改写 → 决定策略 → 插入标签
- 口语化改写规则：断长句、加语气词、调语序、加昵称、允许半句重来、数字写汉字、负面反问改正面表达
- 中文特殊规则：语气词优先于标签、意群呼吸、重点前置昵称、口非心是软化策略

### ✅ 第三步：编写 SKILL.md 和参考文件（已完成）

文件结构：
```
tts-director/
├── SKILL.md                      # 主指令（~150 行）
└── references/
    ├── s2-pro-tags.md             # 标签速查表（~135 行）
    └── examples.md                # 6 个输入输出示例（~180 行）
```

### 🔄 第四步：测试（进行中）

计划使用 Claude Code 进行测试：
- 直接让 Claude Code 读取 skill 文件并标注测试文本
- 或创建自动化测试脚本调用 Claude API
- 将标注结果放入 Fish Audio S2 Pro 试听

### ⬜ 第五步：迭代

根据测试听感反馈优化 skill。

### ⬜ 第六步：打包

使用 skill-creator 打包。

---

## 后续扩展

### 扩展一：ElevenLabs v3 对比（与 Demo 合并）

原计划的 Qwen3-TTS 对比调整为 ElevenLabs v3 对比。ElevenLabs v3 同样支持方括号标签语法，可以直接复用 TTS Director 的标注结果。

### 扩展二：给 Kelivo 提 Issue（待定）

TTS 文本分流功能建议——显示给用户的文本是干净的，发给 TTS 的文本保留标签。

### 🔄 扩展三：交互式 Demo（进行中）

双栏对比 Demo：
- 左栏：Fish Audio S2 Pro
- 右栏：ElevenLabs v3
- 中间：Claude API 实时标注
- 详细规格见 `tts-director-demo-spec.md`

---

## 竞品对比速查

| 维度 | Fish Audio S2 Pro | ElevenLabs v3 | Qwen3-TTS |
|------|------------------|---------------|-----------|
| 情感控制方式 | 内联方括号标签 | 内联方括号标签 | 全局自然语言指令 |
| 控制精度 | 逐词级别 | 逐词级别 | 整段文本 |
| 自动标注 | ❌ 无（TTS Director 填补此缺口） | ❌ 无 | ❌ 无 |
| 环境音 | ❌ 不支持 | ✅ 支持 | ❌ 不支持 |
| 中文支持等级 | Tier 1 | 多语言（70+语言） | 原生 |
| 音色克隆相似度 | 高 | 中等 | 更高（0.95） |
| 标签语法 | `[free-form text]` | `[free-form text]` | 不适用 |
| 许可证 | CC-BY-NC-SA-4.0 | 商业（专有） | Apache 2.0 |
| API 价格 | $15/百万字符 | $100/百万字符 | 免费（DashScope 额度内） |
| 关键参数 | temperature, top_p, speed | stability, style, similarity_boost | instruct（自然语言） |
