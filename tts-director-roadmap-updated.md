# TTS Director Skill 路线图（更新版）

## 项目背景

### 发现的产品缝隙

Fish Audio 的网页端（Drama 3 模型）提供了"自动加标签"功能，可以自动为文本插入情感和韵律标签。但 API 端（`s2.1-pro`）不提供这个功能——开发者需要自己在文本中手动插入标签。

这个 skill 的目标是填补这个缝隙：让 Claude（或任意一个够用的模型）自动分析中文文本的情感结构，在合适的位置插入 Fish Audio `s2.1-pro` 支持的方括号标签，输出可以直接用于 API 调用的标注文本。

### 核心设计原则

前三条是最初实验时形成的，后三条是实测迭代过程中补上的：

1. **克制优先**：对于中文日常对话，过度标注会让语音像在演戏。默认策略是不标注，只在必要时插入标签。
2. **表里一致性判断**：标注密度不取决于情绪强度，而取决于文本表面语义和说话者真实意图之间的距离。表里一致时克制，表里不一致时密集。
3. **文本改写与标签同等重要**：断长句、加语气词、调语序、允许半句重来——这些口语化改写往往比加标签更能改善自然度。
4. **判据必须是标注模型自己能验证的东西**。踩过一次：`[surprised]` 原来的规则是"全知型人设慎用"，但标注时看不到人设说明，而且标注模型和聊天模型是分开的、通常连上一轮对话都拿不到，这条规则等于废掉一个标签。改成"看信息落差方向"之后（新信息是**证实**还是**推翻**了说话者原本的判断），纯靠文本里的词汇痕迹就能判断。**写新规则前先问：标注模型手上有没有做这个判断所需的信息？**
5. **整体持续往"更克制"的方向收**，且都由实听反馈驱动。停顿、呼吸、叹气、耳语这几类都从"鼓励用"改成了"默认不加"。
6. **实测结论要留住证据再改规则**。TTS 生成本身带随机性（`temperature: 0.7`/`top_p: 0.7`），同一段文本每次结果都不一样，"听到一次怪声"不等于"某条规则有问题"。曾经据此加过一条规则，后来发现归因错了、整条 revert 掉。

### 音色与标签的交互效应

实验发现：在加了标签的情况下，不同音色并不容易导致角色脱离（OOC），标签效果相对稳定。关键因素是克隆声音源本身的基调，而非标签与音色的交互。因此 skill 没有采用最初设计的三档音色系统，而是统一处理。

---

## 进度追踪

### ✅ 第一步：Fish Audio 标签实验（已完成）

完成内容：
- 标签分类：情绪类、副语言类、语音语调类、动词短语型、复合描述性标签
- 发现稳定标签子集 vs 不稳定标签
- 密度规则：取决于表里一致性，而非情绪强度
- 音色交互：标签效果跨音色相对稳定
- 中文特殊发现：标点符号可替代标签、口腔微动作词直接触发韵律、"口非心是"软化策略
- 不支持环境音标签和 XML 风格标签
- 大量连续破折号拖长音效果不佳
- 动词标签通常比情绪标签更有效
- 标签堆叠上限为 2 个
- `[thoughtful]` 需搭配语气词才稳定
- 强度调节修饰语（slightly / very / extremely）有效

产出：测试总结文档 + ElevenLabs 技巧参考截图

### ✅ 第二步：设计 Skill 规则（已完成）

核心规则框架：
- 四级工具优先级：标点符号 → 口腔微动作词 → 结构性标签 → 情绪/语调标签
- 五种文本类型：平叙型、知识解释型、情感错位型、情绪急转弯型、高表现力型
- 四步标注流程：通读 → 口语化改写 → 决定策略 → 插入标签
- 口语化改写规则：断长句、加语气词、调语序、加昵称、允许半句重来、数字写汉字、负面反问改正面表达
- 中文特殊规则：语气词优先于标签、重点前置昵称、口非心是软化策略

### ✅ 第三步：编写 SKILL.md 和参考文件（已完成）

四个文件**平铺在仓库根目录**，没有 `references/` 子目录（早期规划里写过这个子目录，实际没有采用）：

```
SKILL.md                  # 主指令：触发条件、标注流程、核心原则
s2-pro-tags.md            # 标签速查表
examples.md               # 输入输出对照样本
polyphone-checklist.md    # 多音字音素控制速查表（按需读取，不必每次加载）
```

### ✅ 第四步：测试（已完成）

用 Claude Code 直接读 skill 文件标注测试文本，标注结果放进 Fish Audio 试听。累计三组长文本实测（知识解释型、陪伴闲聊型、多轮问答型），每组都留了带版本变更记录的存档。后来 Demo 做出来之后，测试改为直接在 Demo 里跑，快得多。

### ✅ 第五步：迭代（已完成主要几轮，仍在继续）

实听反馈驱动的规则修订，按时间顺序：

- **禁止 3 标签堆叠**。原来允许"三个不同类别可以破例"，取消了。
- **停顿类默认不加**。日常对话短句多，标点已经产生停顿；逐句加 `[pause]` 会读成一顿一顿的朗读腔。
- **呼吸/叹气类默认不加**。模型自己就会换气，人为补的呼吸是叠在已有呼吸上，听感是多一声刻意的气音。
- **一个衔接点只放一次"呼吸/静默"事件**。停顿标签的实现自带一次起音前的吸气，后面再跟一个也要靠呼吸实现的标签会连续两次呼吸、变成失真喘息。实测出问题的组合：`[long pause] [inhales softly]`、`[long pause] [hesitate]`、`[long pause] [exhales] [low voice]`。
- **`[pause]`、`[whispering]` 收紧**，从"鼓励用"改成"只在关键处加"。
- **`[surprised]` 的判据从人设改为信息落差方向**，见上面原则 4。
- **给 `[stammering]` 一个入口**。这个标签长期几乎不被触发，因为它只有释义没有触发条件。现在挂在 `[surprised]` 后面：判定为真惊讶就鼓励写成 `[surprised] [stammering]`，`[slightly surprised]` 那一档不接。用**惊讶的强度分档**同时编码"要不要结巴"，避免引入第二套独立判据。
- **术语统一**：说话者（要合成语音的一方）／对方（说话者说话的对象）／用户（调用 skill 的人），三个词固定不混用。

### ✅ 第六步：发布（已完成，方式和原计划不同）

没有用 skill-creator 打包，而是直接开源了整个仓库，四个 skill 文件平铺在根目录，复制进 `.claude/skills/tts-director/` 即可使用（`SKILL.md` 的 frontmatter 里已声明 `name: tts-director`）。

- 仓库：https://github.com/dawnsyo-blip/ttsdirectorskill-chatting
- 在线 Demo：https://ttsdirectorskill-chatting.onrender.com

### ✅ 第七步：浓缩版（新增，已完成）

`SKILL-mini.md`——四个文件（合计约 11000 token）的压缩替代版，约 1800 token，1/6 体量，给 token 敏感的接入场景用。

压缩时的取舍：**标签词表不能砍**。模型不知道 `s2.1-pro` 认哪些标签，`[gently nudging]`、`[with a knowing chuckle]` 这种不是通用常识，猜不出来——砍掉词表是断崖式退化，模型会生成语法对但引擎不认的标签。**可以砍的是推理和示例**，压成口号后是渐进退化，模型能靠自身能力补一部分，最主要的损失是"标注密度感"。所以 200 token 那种量级做不到（光标签名本身就要三四百 token），1500–2500 token 是性价比拐点。

---

## 后续扩展

### ✅ 扩展一：ElevenLabs v3 对比（已并入 Demo）

原计划的 Qwen3-TTS 对比调整为 ElevenLabs v3 对比。ElevenLabs v3 同样支持方括号标签语法，可以直接复用 TTS Director 的标注结果。Demo 里两栏各有一个"标注/原文"开关，可以对比"有 skill vs 没 skill"，也可以对比同一份标注在两个引擎上的差异。

### ⬜ 扩展二：给 Kelivo 提 Issue（待定）

TTS 文本分流功能建议——显示给用户的文本是干净的，发给 TTS 的文本保留标签。

Kelivo 这类现成前端目前还没法直接装这个 skill，因为需要在"模型生成回复"和"喂给 TTS"之间自己插入一次额外的标注调用。更现实的路径是自建前端，`demo/` 本身就是一份可以照抄的参考实现。

### ✅ 扩展三：交互式 Demo（已完成并上线）

本地 Node/Express + 原生 HTML/CSS/JS，部署在 Render 免费档。

- 左栏 Fish Audio `s2.1-pro`，右栏 ElevenLabs，中间是标注引擎
- **标注引擎不锁定某一家**：可选 Claude（官方 SDK，带 prompt caching）或任意 OpenAI 兼容接口（自填 Base URL + Key + 模型名）。实测 `deepseek-v4-flash` 做标注模型效果不错
- 标注流式返回，边收边填进文本框
- API key 只存浏览器 localStorage，服务端不落盘，所以这个 Demo 可以整个分享出去，对方用自己的 key 跑
- 生成的语音可下载，每个引擎保留最近 5 条历史

原本的规格文档假设做成 Claude Artifact、固定调用 Anthropic API、输出 JSON 格式，这三点最后都没这么实现（Artifact 的 CSP 不放行这些 API；标注引擎做成了可选；JSON 换成了分隔符文本，因为模型对标注文本里的换行/引号转义不可靠）。实现细节和踩过的坑以 `CLAUDE.md` 和 `demo/README.md` 为准。

---

## 竞品对比速查

核实日期 2026-09-06。价格和条款随时会变，正式决策前请以各家官网为准。

| 维度 | Fish Audio `s2.1-pro` | ElevenLabs v3 | Qwen3-TTS |
|------|------------------|---------------|-----------|
| 情感控制方式 | 内联方括号标签 | 内联方括号标签 | 自然语言音色描述 + 情感控制 |
| 控制精度 | 逐词级别 | 逐词级别 | 整段为主，另有全局停顿控制 |
| 自动标注 | ❌ 无（TTS Director 填补此缺口） | ❌ 无 | ❌ 无 |
| 环境音 | ❌ 不支持 | ✅ 支持（音效类 audio tag） | ❌ 不支持 |
| 语言覆盖 | 83 种 | 70+ 种 | 10 种 |
| 标签语法 | `[free-form text]` | `[free-form text]` | 不适用 |
| 部署方式 | 仅云 API | 仅云 API | 开源权重，可自托管（1.7B / 0.6B），也有云 API |
| 许可证 | 商业条款（专有） | 商业（专有） | Apache 2.0 |
| API 价格 | **$15 / 100 万 UTF-8 字节** | $0.10 / 1000 字符（≈ $100/100 万字符） | 自托管免费；阿里云百炼托管按区域计价 |
| 免费档 | `s2.1-pro-free`（同一模型，无硬性字数上限，无 SLA，>$1M ARR 需联系官方） | 按订阅额度，无永久免费 API | 自托管即免费 |
| 关键参数 | temperature, top_p, speed | stability, style, similarity_boost | instruct（自然语言） |

**几条容易踩的坑：**

- **Fish Audio 按 UTF-8 字节计费，不是按字符。** 一个中文字是 3 字节，所以做中文时实际成本约 **$45 / 100 万字**，不是 $15。这条对本项目尤其重要，原表写成"$15/百万字符"是低估了三倍。
- **CC-BY-NC-SA-4.0 是 OpenAudio S1-mini 开源权重的许可证，不是 API 的。** 走 API 就是走 Fish Audio 的商业条款；免费档也允许商用，但年营收超过 100 万美元的产品需要先联系官方。原表把权重许可证套到了 API 上。
- **Qwen3-TTS 的 Apache 2.0 指的是开源权重**，自托管商用没问题；但用阿里云百炼的托管 API 是付费的，原表写"免费（DashScope 额度内）"不准确。另外它的能力已经不只是"整段自然语言指令"了，现在有音色设计、情感控制和全局停顿控制。
- 原表里"音色克隆相似度"一行（高 / 中等 / 0.95）查不到可靠出处，已删除。
